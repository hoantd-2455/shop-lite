import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      description="Đây là trang đăng nhập. Authentication sẽ được triển khai ở phần Auth.js của module."
      title="Đăng nhập"
    />
  );
}
